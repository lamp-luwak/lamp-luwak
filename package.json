{
  "name": "@impress/lerna-root",
  "private": true,
  "author": "Viacheslav Bereza <mail@betula.co>",
  "license": "MIT",
  "repository": {
    "url": "https://github.com/betula/impress"
  },
  "scripts": {
    "bootstrap": "npx lerna bootstrap && npm run build",
    "build": "npx lerna run build",
    "test": "npx lerna run test",
    "lint": "npx lerna run lint",
    "clean": "npx lerna run clean",
    "publish": "npm run lint && npm run build && npm run test && npx lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
