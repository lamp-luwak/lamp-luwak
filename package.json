{
  "name": "@impress/lerna-root",
  "private": true,
  "author": "Viacheslav Bereza <mail@betula.co>",
  "license": "MIT",
  "repository": {
    "url": "https://github.com/betula/impress"
  },
  "scripts": {
    "update": "npx lerna bootstrap",
    "bootstrap": "npm i && npx lerna bootstrap && npm run build && npm run test",
    "build": "npx lerna run build",
    "test": "npx lerna run test",
    "lint": "npx lerna run lint",
    "clean": "npx lerna run clean",
    "publish": "npm run clean && npm run lint && npm run build && npm run test && npx lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
