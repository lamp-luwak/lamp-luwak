{
  "name": "@impress/real-world-root",
  "version": "0.3.0",
  "private": true,
  "scripts": {
    "server-api": "npm run dev --prefix server-api",
    "client-web": "npm run dev --prefix client-web",
    "dev": "npx concurrently -k --names \"API,WEB\" -c \"white.bold,magenta.bold\" \"npm run server-api\" \"npm run client-web\""
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
