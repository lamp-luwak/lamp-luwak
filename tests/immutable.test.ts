import { create, modify } from "../src";

test("Should modify store", () => {
  // ...
});

///
//
// Упростить пример ssr, убрать вложенные сторы, пусть это (вложенные сторы) будет advanced секция
// лучше сделать простой пример с одним стором и всё.
// И List вместе с ListItem переместить в index компонент, что бы сократить объем файлов
//
// Назвать функцию const data = ssr(async () => {
//   await provide(User).fetch();
// });
// как ssr
//
///
